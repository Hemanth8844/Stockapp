import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  tag: {
    backgroundColor: "#ddd",
    borderRadius: 12,
    paddingHorizontal: 8,
    paddingVertical: 4,
    marginRight: 6,
  },
  tagText: {
    fontSize: 12,
  },
});

export default styles
