import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  container: { padding: 10, alignItems: "center" },
  label: { fontWeight: "bold" },
  value: { fontSize: 14 },
  current: { fontSize: 14, color: "tomato", marginTop: 4 },
});

export default styles